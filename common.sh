GROUP_ID="uk.ac.rhul.cs.dice.vacuumworld"
ARTIFACT_ID="vwcommon"
VERSION="1.0.1"
JAR_NAME=${ARTIFACT_ID}-${VERSION}.jar
TARGET_DIR="target"
WORKING_DIR=$(pwd)
JAR_FULL_PATH=${WORKING_DIR}/${TARGET_DIR}/${JAR_NAME}
CLEAN_SCRIPT_NAME="clean.sh"
CLEAN_SCRIPT_PATH=${WORKING_DIR}/${CLEAN_SCRIPT_NAME}
PATH_WITH_SLASHES=${GROUP_ID//\./\/}/${ARTIFACT_ID}/${VERSION}
DEP_DIR=${HOME}/.m2/repository/${PATH_WITH_SLASHES}
MAVEN_INSTALLED_JAR_PATH=${DEP_DIR}/${JAR_NAME}